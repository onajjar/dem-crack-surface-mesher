# Container output

Docker Compose mounts this directory at `/app/_runtime` inside the container.
The committed examples therefore write generated meshes and reports here while
the container itself remains disposable.

Generated files below this README are intentionally ignored by Git. Keep or
archive any results needed for a study before deleting them locally.

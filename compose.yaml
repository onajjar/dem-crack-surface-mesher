services:
  mesher:
    build:
      context: .
      dockerfile: Dockerfile
    image: dem-crack-surface-mesher:local
    init: true
    user: "${HOST_UID:-1000}:${HOST_GID:-1000}"
    volumes:
      - type: bind
        source: ./container-output
        target: /app/_runtime
